type User {
    id: ID!
    email: String!
    name: String
}

type Query {
    users: [User]
    getUserById(id: ID!): User
    getUserByEmail(email: String!): User
}

type Mutation {
    createUser(email: String!): User
}

schema {
    query: Query
    mutation: Mutation
}
